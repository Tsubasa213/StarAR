# 卒研進捗まとめ：StarAR

更新日: 2026-07-14  
参照元: [卒研に関する共有チャット](https://chatgpt.com/share/6a4d0d72-95e8-83e8-906b-3aa7d9ec9a61) と現在のワークスペース

## 1. 最終目的

スマートフォンのカメラを通して見える現実空間に、**観測地点・現在時刻・端末の向きに対応した本物の星空**を重ねて表示するAR天体観察アプリを完成させる。

単に星を球面上へ配置するだけではなく、次の3要素を同時に反映することが最終目標である。

1. 観測地点の緯度・経度
2. 現在時刻から求めた恒星時
3. 端末のコンパス／ジャイロによる方位・姿勢

最終的には、ユーザーが空へ端末を向けたとき、実際の星の位置とAR上の星の位置が一致し、星・星座・天の川を観察できる状態を目指す。

## 2. アプリの構成方針

```text
赤道座標（RA / Dec）を持つ星データ
                │
                ▼
        StarManager / StarGenerator
        ・CSVを読み込む
        ・星を球面上へ生成する
        ・等級で大きさを変える
        ・B-V色指数で色を変える
                │
                ▼
        CelestialSphere
        ・観測地点の緯度
        ・恒星時による天球の回転
                │
                ▼
        SkyRoot
        ・端末の方位／姿勢（コンパス・ジャイロ）
                │
                ▼
        Main Camera / AR表示
```

Scene上の大枠は `SkyRoot` の下に星・天の川・方位マーカーなどを置く構成で、チャット上では次の責務分離を維持する方針になっている。

| オブジェクト | 担当する回転・表示 |
|---|---|
| `SkyRoot` | コンパス／端末姿勢による北方向合わせ |
| `CelestialSphere` | 緯度による傾き＋恒星時による天球回転 |
| `StarManager` | 星データの読み込みと星オブジェクト生成 |
| `GalacticRoot` | 天の川など銀河系の表現 |
| `NorthMarker` | 方位合わせのデバッグ表示 |

## 3. ここまでにできていること

### 3.1 Unity／ARプロジェクトの基盤

- Unity `6000.4.3f1` のプロジェクト。
- AR Foundation、ARCore／ARKit、XR Interaction Toolkitを導入済み。
- `Assets/Scenes/MainARScene.unity` にAR Session、XR Origin、Main Cameraなどの基本構成がある。
- Unity Input Systemの `PlayerInputAction.inputactions` と生成済みの `PlayerInputAction.cs` がある。
- エディタ上で確認するための `PlayerCameraController` が作成されている。ただし、これは本番AR機能ではなく開発用カメラである。

### 3.2 星データの取得・整形

- VizieRの星表をTAP／SQLで取得する方針を検討した。
- CSVが取得できない段階を経て、`vizier_votable.tsv` をもとに星データを整理した。
- `stars.csv`、`stars_5.csv`、`stars_6.csv` が `Assets/Resources` に存在する。
- データの主な形式は次の通り。

```text
ra,dec,id,name,mag,bv
```

- `ra` と `dec` は赤経・赤緯、`mag` は等級、`bv` はB-V色指数。
- `stars_5.csv` の `FormatException` は、CSVの異常ではなく、日本語環境の小数点解釈が原因と判断され、`CultureInfo.InvariantCulture` を使う方針になった。

### 3.3 星の生成と見た目

`Assets/Scripts/Star/StarGenerator.cs` には、次の実装がある。

- `Resources.Load<TextAsset>("stars_6")` による星表読み込み。
- 赤経・赤緯を球面座標へ変換。

```text
x = R cos(Dec) cos(RA)
y = R sin(Dec)
z = R cos(Dec) sin(RA)
```

- 球面半径は現在 `100`。
- `CoreSphere.prefab` を星のPrefabとして生成。
- 星を指定された `celestialSphere` の子として生成する設計。
- 等級が明るい星ほど大きく、暗い星ほど小さくする。
- B-V色指数から青白・白・黄色・赤系の色を選び、マテリアルの `_BaseColor` に反映する。
- チャット上ではInspectorの参照設定を直した後、星が生成されるところまで到達している。

### 3.4 天の川の表現

- `Assets/Shaders/MilkyWay.shader` を作成済み。
- 球面の赤道付近を天の川の帯として扱う。
- 3DノイズとFBMで雲状の濃淡を作る。
- コア色・外縁色・帯の厚さ・ノイズスケール・全体強度をマテリアルから調整できる。
- `GalacticRoot` 配下に `MilkyWay` がある。
- チャットでは、URP Lit、カラーピッカー、Global VolumeのBloom、星Prefabの見え方を調整していた。
- Bloomは天の川には作用するが星には十分作用していない可能性があり、星マテリアルのHDR／Emission、カメラのHDR、Volumeの設定を検証する必要がある。

### 3.5 GPS・恒星時の実装準備

次のスクリプトが作成済みである。

- `Assets/Scripts/Sensor/LocationProvider.cs`
  - `Input.location` から緯度・経度を取得する。
  - `IsReady`、`Latitude`、`Longitude` を公開する。
- `Assets/Scripts/Sensor/SiderealTime.cs`
  - UTCからユリウス日を求める。
  - GMSTと経度から地方恒星時（LST）を求める。
- `Assets/Scripts/Sensor/CelestialSphereController.cs`
  - 緯度によるX回転と、LSTによるY回転を合成する。
- `Assets/Scripts/Sensor/CompassSensor.cs`
  - コンパスとジャイロを有効化し、方位を保持する。
- `Assets/Scripts/Sensor/ARSkyController.cs`
  - クラス名は `SkyController`。
  - `SkyRoot` と `NorthMarker` をコンパスの真北に合わせる。
- `Assets/Scripts/Sensor/SkyRotation.cs`
  - 開発用に時間を早回しして天球を回転させる旧方式。

## 4. 現在の進捗状況

### チャット上の到達点

- 星表を読み込み、星を生成する仕組みは動作確認まで進んだ。
- 星を `CelestialSphere` の子に生成する方針を確認した。
- 次の実装としてGPSによるデフォルト天球位置の変更に着手した。
- 緯度だけを先に反映し、その後に恒星時を追加する段階的な実装方針を決めた。
- 最終的な回転順序を次のように定めた。

```text
赤道座標
  ↓
緯度による傾き
  ↓
恒星時による自転
  ↓
コンパスによる北合わせ
  ↓
AR表示
```

### 現在のワークスペースで確認できる状態

| 項目 | 状態 | 根拠・注意点 |
|---|---|---|
| Unity／AR基盤 | 構築済み | `MainARScene`、AR Foundation、XR Originが存在 |
| 星CSV | あり | `Assets/Resources/stars.csv`、`stars_5.csv`、`stars_6.csv` |
| 星生成コード | 実装済み | `StarGenerator.cs` に座標変換・生成・明るさ・色の処理あり |
| 星Prefab | あり | `Assets/Prefabs/CoreSphere.prefab` |
| 星生成のScene接続 | **未確認／要修正** | `MainARScene` の `StarGenerator.celestialSphere` が `{fileID: 0}` |
| GPS取得コード | 作成済み | Scene上への配置・権限・実機確認は未完了 |
| 恒星時計算 | 作成済み | `SiderealTime.cs` は存在するが、動作検証は未完了 |
| 天球回転制御 | 作成済み | `CelestialSphereController.cs` は存在するが、Scene接続を要確認 |
| コンパス方位合わせ | 一部実装 | `SkyController` は `SkyRoot` と `NorthMarker` に接続済み |
| センサー責務の整理 | 未完了 | `SkyController` と `CompassSensor` が別々にセンサーを読む |
| 開発用時間回転 | 旧方式が残存 | `SkyRotation.cs` と恒星時制御を同時に有効化しない |
| 天の川 | 表現実装済み | Bloomと星の発光の見え方が未検証 |
| 位置・時刻・端末姿勢を統合したAR表示 | **未完了** | 卒研の中心となる検証段階が残っている |

## 5. 直近の重要な課題

優先度順に並べると次の通り。

### P0：実行可能な基準状態を確立する

1. `StarManager` の `StarGenerator.celestialSphere` に、実際に星をぶら下げるTransformを設定する。
2. Scene上の階層を確認し、`CelestialSphere` という役割のTransformを明確にする。現在のSceneでは、チャットで使っていた名前と実ファイル上のオブジェクト名に差がある可能性がある。
3. `StarGenerator` のCSV nullチェックを、`csvFile.text` を参照する前に行う。
4. CSVの列数・ヘッダー・改行コードを検証し、異常行を一括終了ではなく安全に扱えるようにする。
5. Unity Consoleで、読み込み件数・生成件数・例外内容を確認する。

### P1：GPSとデフォルト位置

- `LocationProvider` をSceneへ配置する。
- Android実機で位置情報権限とGPS無効時の挙動を確認する。
- GPSが取れない場合のデフォルト地点（例：大阪）と表示状態を決める。
- 実際の緯度を `CelestialSphere` の傾きへ反映する。

### P2：恒星時による天球回転

- `CelestialSphereController` をSceneへ配置し、対象Transformを設定する。
- `SkyRotation` の開発用回転を無効化または撤去する。
- 固定した日時・経度・緯度で既知の星の位置を確認する。
- 回転の符号、軸、座標系の向きを実機または星図と照合する。

### P3：コンパス・ジャイロとの統合

- `SkyRoot` は端末姿勢だけ、`CelestialSphere` は緯度＋恒星時だけを変更するよう統一する。
- `CompassSensor` を使うのか、`SkyController` 内で直接読むのかを決め、二重実装をなくす。
- `trueHeading` が取得できない端末・権限・磁気干渉時のフォールバックを設計する。
- `NorthMarker` を使って北合わせを確認する。

### P4：卒研としての評価

最低限、次の条件を固定して評価できるようにする。

- 複数の観測地点（緯度・経度）
- 複数の日時
- 北・東・南・西へ端末を向けた状態
- 明るい基準星の表示位置
- 星生成数とフレームレート
- GPS／コンパスが使えない場合の動作

比較対象、誤差の定義、評価場所、提出期限は共有チャットでは確定していないため、指導教員と決める必要がある。

## 6. 完成条件

- [ ] 起動時に星、天の川、ARカメラが表示される。
- [ ] 星データを再生成せずに、指定した星表を安定して読み込める。
- [ ] 星の位置が赤経・赤緯から一貫した座標系で生成される。
- [ ] 観測地点の緯度・経度が取得できる。
- [ ] 現在時刻から恒星時を求め、星空が正しい速度・方向で回転する。
- [ ] コンパス／ジャイロで北方向と端末の向きが反映される。
- [ ] `SkyRoot`、`CelestialSphere`、`StarManager` の責務が干渉しない。
- [ ] 実機で既知の星の位置を星図などと比較し、誤差を記録できる。
- [ ] GPS・センサー未使用時のデフォルト動作が定義されている。
- [ ] 生成星数を増やした場合の処理時間と表示性能を確認できる。

## 7. 現在の注意事項

- ワークスペースには、今回の文書作成前から未コミットのScene・ProjectSettings・復旧用ファイルがある。これらを勝手に破棄・リセットしない。
- `Library`、`Temp`、生成された `.csproj` はUnityの生成物であり、通常は直接編集しない。
- 共有チャット上の動作確認結果と、現在のSceneファイルの参照状態は一致していない。以後は必ずUnity EditorのInspectorとPlay Modeで確認する。
- まず「星が生成される基準状態」を復元してから、GPS、恒星時、コンパスの順に統合する。
## 8. 2026-07-14 時点の恒星時回転実装

### 今回完了したこと

- `Assets/Scripts/Sensor/SiderealTimeRotation.cs` を追加し、`MainARScene` の `SkyRoot` にアタッチした。
- 起動時の天球配置を基準に、起動後の恒星時の経過分だけ `SkyRoot` をY軸回転させるようにした。
- `SkyRoot` 配下の天の川、星、星座、ラベルを同じ回転で動かせる構成にした。
- 恒星時が0時をまたぐ場合と、長時間起動する場合の回転ジャンプを防ぐため、フレーム間差分を補正して連続積算する方式にした。
- `StarGenerator.celestialSphere` を暫定的に `SkyRoot` に接続した。現在のScene構造では、これにより生成された星も天球回転の対象になる。
- `SkyController` が `SkyRoot.localRotation` を直接上書きしないようにした。コンパス回転は次段階で別の回転層に統合する。
- `SiderealTime` の計算にミリ秒を含め、経過時間による回転をより滑らかにした。

### 現在の到達点と制限

- 恒星時に応じて天球全体を回転させる第1段階は実装済み。
- 現在は起動時の天の川配置を基準にした相対回転であり、GPSの緯度・経度から絶対的な天球姿勢を決める処理は未接続。
- 次は `LocationProvider` から現在位置を取得し、緯度による高度方向の補正、経度を含む恒星時、端末の東西南北方位を別レイヤーとして統合する。

### 検証

- `Assembly-CSharp.csproj` の再ビルド: 成功（0エラー、既存パッケージ由来の警告のみ）。
- Unity EditorのPlay Mode、実機GPS、コンパスを使った見た目の検証は未実施。
## 9. デバッグ高速化と現在地取得の実装

### ファイルごとの変更

- `Assets/Scripts/Sensor/SiderealTimeRotation.cs`
  - `siderealTimeScale` を追加した。
  - `1` が実時間、現在のScene設定 `600` では実時間1秒につき恒星時約10分進む。
  - 恒星時の計算自体は変更せず、フレーム間の経過角度だけを倍率で拡大している。
- `Assets/Scripts/Sensor/LocationProvider.cs`
  - GPSの権限・初期化・タイムアウト・更新処理を整理した。
  - `Latitude`、`Longitude`、`IsReady`、`StatusMessage` を公開し、他のクラスが現在地を参照できるようにした。
  - Unity EditorではGPSが使えないため、大阪（緯度34.693738、経度135.502165）をフォールバックとして使用する。
  - Android実機ではFine Location権限を要求し、実際のGPSを使用する。
- `Assets/Scenes/MainARScene.unity`
  - `LocationProvider`専用GameObjectを追加した。
  - `CelestialSphereController.siderealTimeScale` を1に設定し、実時間の恒星時回転に戻した。
- `ProjectSettings/ProjectSettings.asset`
  - 位置情報利用目的の説明文を設定した。

### 重要な設計点

- `LocationProvider` は緯度・経度を提供するだけで、天球Transformを直接回転させない。
- 次の実装では、現在地の緯度を天球の高度方向へ反映し、恒星時を使った回転と端末の東西南北方位を別の回転層として統合する。
- デバッグ時は `siderealTimeScale` を600に変更できるが、通常動作では1を使用する。

### 検証

- `Assembly-CSharp.csproj` の再ビルド: 成功（0警告、0エラー）。
## 10. 卒業研究テーマの発展方針

### 10.1 研究テーマ（仮）

**実カメラ画像中の星と天文計算によるAR星空の統合による、ジャイロ姿勢・方位・座標誤差の補正**

### 10.2 研究の最終目的

本アプリの最終目的は、現在地・時刻・端末姿勢から生成した星空をAR表示するだけではない。実際のカメラ映像に写っている星を画像認識し、同じ時刻・場所における天文計算上の星の位置と対応付けることで、端末のジャイロやコンパスだけでは発生する姿勢誤差を補正することである。

具体的には、次の処理を一つのループとして実現する。

```text
現在地・時刻・星表
        ↓
天球座標からAR画面上の星位置を計算
        ↓
実カメラ画像から星候補を検出
        ↓
実画像の星とAR星を対応付け
        ↓
対応点からカメラ姿勢の誤差を推定
        ↓
ジャイロ・コンパス・AR座標を補正
        ↓
補正後の星空を再表示
```

### 10.3 背景と問題意識

ジャイロセンサは短時間の姿勢変化には強いが、積分誤差によるドリフトが蓄積する。コンパスは磁気ノイズ、建物、鉄製構造物、端末の持ち方による影響を受けやすい。GPSは緯度・経度を取得できるが、数mから数十mの位置誤差があり、姿勢そのものは取得できない。

そのため、GPS・ジャイロ・コンパスだけで星空を重ねると、時間経過や環境によってARの星が実際の星からずれる可能性がある。本研究では、実カメラに写った星を視覚的な基準として利用し、センサだけでは観測できない姿勢誤差を補正する。

### 10.4 座標系の整理

実装上は、次の座標系を混同しないことが重要である。

| 座標系 | 内容 | 主なデータ |
|---|---|---|
| 赤道座標系 | 星表が持つ天文学的な座標 | 赤経RA、赤緯Dec |
| 地平座標系 | 観測地点から見た空の方向 | 方位角Azimuth、高度Altitude |
| ARワールド座標系 | Unityの3D空間 | World Position、World Rotation |
| カメラ座標系 | 実カメラから見た方向 | Camera Forward、画面座標 |
| センサ姿勢座標系 | ジャイロ・コンパスが返す端末姿勢 | Quaternion、Heading |

星表のRA・DecをそのままUnityワールド座標として扱うのではなく、恒星時と観測地点の緯度を使って地平座標へ変換し、端末の姿勢を使ってARカメラ座標へ変換する必要がある。

### 10.5 現在実装した天球姿勢計算

`Assets/Scripts/Sensor/CelestialSphereController.cs` を天球回転の正式な担当にした。`LocationProvider` の緯度・経度を使い、`SiderealTime` から恒星時を計算し、次の回転を合成する。

```text
緯度回転       = Quaternion.Euler(latitude - 90, 0, 0)
恒星時回転     = Quaternion.Euler(0, -localSiderealTime, 0)
天球回転       = alignment補正 × 恒星時回転 × 緯度回転
```

`SiderealTimeRotation` と同じTransformを同時に回転させると競合するため、`MainARScene` では旧コンポーネントを無効化し、`CelestialSphereController` だけを有効にしている。

### 10.6 画像認識による星の検出

実カメラ画像から星候補を取得する段階では、次の処理を想定する。

1. カメラ画像を取得する。
2. RGB画像を輝度画像またはHSV系の画像へ変換する。
3. 夜空の背景から明るい点をしきい値処理で抽出する。
4. 二値画像の連結成分を調べ、星候補の中心位置・面積・輝度・形状を得る。
5. 飛行機、街灯、看板、ノイズ、雲などの星ではない候補を除外する。
6. フレーム間追跡によって、一時的なノイズを除外する。

最初からすべての星を認識するのではなく、明るい星を数個から十数個に限定する方が実験しやすい。星の大きさはカメラの露光・フォーカス・HDR・圧縮に大きく左右されるため、画素面積だけで等級を正確に推定することは難しい。初期段階では、位置と相対的な明るさを主な特徴量として扱う。

### 10.7 AR星との対応付け

検出した実画像の星と、星表から計算したAR星を対応付ける必要がある。単純に画面上の距離だけで対応させると、姿勢誤差が大きい場合に誤対応が起きる。そのため、次のような幾何学的特徴を使う。

- 星同士の角距離
- 三角形・四角形などの星の組み合わせ
- 明るい星の相対的な明るさ
- 星座を構成する形状
- 前フレームからの移動量

星の組み合わせの角距離は、カメラの姿勢が変わっても球面上で保存される。そのため、星の絶対画面位置だけでなく、複数星間の距離や角度を使うと、初期姿勢誤差に強い対応付けができる。

### 10.8 姿勢誤差の推定

実画像上の星の方向ベクトルと、天文計算から得た星の方向ベクトルが対応したら、両者を最もよく一致させる回転を求める。候補となる方法は次の通りである。

- RANSAC：誤対応を外れ値として除外する。
- Wahba問題：対応する単位ベクトル群を最小二乗で整合させる。
- Kabsch法：対応点群の最適回転を求める。
- Perspective-n-Point：画面投影モデルを使ってカメラ姿勢を推定する。

研究初期では、星の対応数を3点以上確保し、RANSACとベクトル回転推定を組み合わせる構成が実装しやすい。推定結果は、現在のARカメラ姿勢と比較して補正クォータニオンに変換する。

```text
q_error = q_visual_reference × Inverse(q_sensor_estimate)
```

ただし実際の実装では、Unityの座標軸、カメラ前方軸、端末の上方向、画面回転を統一してからクォータニオンを計算する必要がある。

### 10.9 ジャイロ・コンパス・画像認識の統合

画像認識結果は毎フレーム安定して得られるとは限らない。そのため、画像認識だけで姿勢を置き換えるのではなく、センサ姿勢を予測値、画像認識を観測値として扱う。

```text
ジャイロ積分      → 高速で滑らかな予測
コンパス・GPS     → 絶対方位・位置の補助
星画像認識        → 視覚的な姿勢誤差の観測
補完フィルタ等    → 3つの情報を統合
```

実装候補は、まず補完フィルタまたはQuaternion.Slerpによる重み付き補正である。画像認識の信頼度が高いときだけ補正量を大きくし、検出星数が少ない、対応誤差が大きい、雲や街明かりが多い場合は補正量を小さくする。

将来的には、拡張カルマンフィルタやパーティクルフィルタを検討できる。ただし卒業研究としては、まず補完フィルタで「画像認識を加えるとジャイロドリフトが減少するか」を検証できる状態を作ることを優先する。

### 10.10 研究上の仮説

1. GPS・恒星時・ジャイロだけで表示した星空は、時間経過とともに姿勢誤差が蓄積する。
2. 実カメラ画像中の星とAR星を対応付けることで、ヨー・ピッチ・ロールの誤差を推定できる。
3. 星の対応付けによる視覚補正を定期的に適用すると、ジャイロ単独よりも長時間の表示誤差を小さくできる。
4. コンパスが不安定な環境でも、十分な星が検出できれば方位の補正が可能になる。
5. 都市部の明るさ、雲、露光条件、検出星数が補正精度に影響する。

### 10.11 評価方法

実験では、同じ地点・同じ時刻条件で、次の方式を比較する。

| 比較方式 | 内容 |
|---|---|
| A | GPS・恒星時のみ |
| B | GPS・恒星時・ジャイロ |
| C | GPS・恒星時・ジャイロ・コンパス |
| D | GPS・恒星時・ジャイロ・画像認識補正 |

記録する評価値は次の通りである。

- 実星とAR星の角度誤差
- ヨー・ピッチ・ロールの補正量
- 時間経過によるドリフト量
- 星の検出数と正しい対応数
- 対応付けの信頼度
- 補正処理のフレーム時間
- 屋外・都市部・暗所など環境ごとの成功率

評価では、画面上のピクセル距離だけでなく、カメラの画角から角度誤差へ変換して比較する。これにより、解像度や端末が異なる場合にも結果を比較しやすくなる。

### 10.12 段階的な実装計画

1. 現在地・恒星時・緯度を使った天球姿勢計算を完成させる。
2. 画面上のAR星の位置が天文計算と一致することを確認する。
3. 実カメラ画像から明るい星候補を検出する。
4. 3個以上の星の組み合わせでAR星との対応付けを行う。
5. 対応点から姿勢誤差クォータニオンを推定する。
6. 推定結果をジャイロ姿勢へ少量ずつ適用する。
7. センサのみの場合と画像補正ありの場合でドリフトを比較する。
8. 環境条件を変えて検出率・補正精度・処理時間を評価する。

### 10.13 現時点での未実装部分

- 実カメラ画像からの星検出
- 星候補と星表データの対応付け
- 姿勢誤差の推定
- ジャイロ姿勢への視覚補正
- 補完フィルタ、カルマンフィルタ等によるセンサ統合
- 実星とAR星の角度誤差を測定する評価画面

したがって、現在のアプリは「天文データ・現在地・恒星時からAR星空を生成する基盤段階」であり、今後は「実カメラ画像を使ってそのAR星空を補正する研究段階」へ進む。
## 11. AR画面の明るさフィルター

### 実装内容

- `Assets/Scripts/Debug/ARBrightnessController.cs` を追加した。
- `MainARScene` の `Global Volume` にコンポーネントを追加した（既存シーンとの互換性のため参照を保持しているが、露出補正には使用しない）。
- `Main Camera` のURPポストプロセスを無効化し、通常のカメラ描画を使用する。
- Androidのカメラ背景だけに黒フィルターを適用し、スライダーで不透明度0〜100%を調整できる。初期値は80%で、最大値ではカメラ背景が完全な黒になる。

### 仕組み

`ARBrightnessController` はURPの露出補正を使用せず、`ARCameraBackground.customMaterial` に `ARCoreBackgroundWithBlackFilter` を設定する。シェーダーがカメラテクスチャの色だけを暗くし、AR星・天の川が描画される前に処理を完了する。

現在の描画順は「カメラ映像 → カメラ用フィルター → AR星・天の川 → スライダーUI」である。そのため、カメラ映像の白飛びを表示上抑えながら、AR星の明るさを維持できる。スライダーはカメラ背景用マテリアルの濃度だけを変更する。

### 今後の改善

現状はAR星の視認性を優先し、カメラ背景だけにフィルターを適用している。将来iOSにも対応する場合は、ARKit用のカメラ背景シェーダーを追加する必要がある。

Android版ではARCoreのカメラテクスチャ形式、端末回転用の表示変換、OpenGLES 3／Vulkanの描画経路に対応したシェーダーを使用する。
## 12. Android限定の明るさフィルター

- `ARBrightnessController` は `UNITY_ANDROID && !UNITY_EDITOR` の場合だけ、黒フィルターとスライダーUIを有効にする。
- カメラ背景用シェーダーで処理するため、画面全体を覆うImageは生成しない。スライダーはカメラ背景の濃度だけを調整する。
- WindowsおよびUnity EditorではARカメラ背景を無効化し、Main Cameraを黒のSolid Colorに設定する。
- Androidの黒フィルター不透明度はスライダーで0〜100%に調整でき、初期値は80%である。最大値ではカメラ背景が完全な黒になる。
- 現段階のフィルターはAndroidのカメラ背景だけに適用される。AR星・天の川・スライダーUIはフィルターの影響を受けない。

## 2026-07-21 現在時刻による恒星時同期

- `CelestialSphereController` は毎フレーム `DateTime.UtcNow` を使って現在の恒星時を再計算する。
- 通常モード（`siderealTimeScale = 1`）ではフレーム差分を蓄積せず、現在時刻から直接天球回転を決めるため、アプリの一時停止・再開や端末時刻の変更後も星の位置を再同期できる。
- `siderealTimeScale` を1以外にした場合は、従来どおりデバッグ用の高速回転として扱う。
- `dotnet build Assembly-CSharp.csproj --no-restore -t:Rebuild` はエラー0件で完了した。Unity EditorのPlay ModeおよびAndroid実機での見た目確認は未実施。

## 2026-07-21 ジャイロを基本姿勢入力に追加

- `SkyController` が `Input.gyro.rotationRateUnbiased` を取得し、回転速度をフレームごとに積分して端末の相対回転を計算する。
- `SkyController` は `Main Camera` の回転を端末姿勢で制御し、ARの `TrackedPoseDriver` と開発用の `PlayerCameraController` を無効化する。
- `CelestialSphereController` は `SkyRoot` 側で緯度・恒星時を担当するため、カメラのジャイロ回転と天文計算の回転が競合しない。
- AndroidのARカメラ背景や平面認識が停止しても、Unityの更新が継続していればVRに近い視点回転をジャイロで継続する。
- ジャイロ座標変換、回転方向、長時間利用時のドリフトはAndroid実機での確認が必要。

## 2026-07-21 Android黒背景テストモード

- AndroidでもWindowsと同じく、`Main Camera` の背景を黒の `Solid Color` にし、`ARCameraBackground` を無効化するテストモードを追加した。
- ARセッション、位置情報、ジャイロは停止しないため、カメラ映像なしで星空とセンサー入力だけを確認できる。
- `MainARScene` の `ARBrightnessController.useSolidBlackBackgroundForAndroidTest` は有効化済み。

## 2026-07-21 AR標準シーンとの姿勢競合を分離

- `MainARScene` の `ARSession.matchFrameRate` を無効化し、ARカメラフレーム待ちでアプリ更新が止まる可能性をなくした。
- Main Cameraの `TrackedPoseDriver` はトラッキング喪失時に最後の姿勢を保持しない設定へ変更した。
- Android黒背景テスト時は `ARSession`、`ARCameraManager`、`ARCameraBackground`、`TrackedPoseDriver` を無効化し、Main Cameraをジャイロだけで回転させる。
- これにより、カメラ画像・AR映像トラッキング・AR姿勢更新と、ジャイロによる星空表示を分離した。

## 2026-07-21 Input Systemセンサー経路の修正

- `ProjectSettings` は新Input System専用（`activeInputHandler: 1`）だったが、ジャイロ処理が旧APIの `Input.gyro` を使っていた。
- Androidのネイティブセンサーイベントは発生していたものの、Unity側の読み取り経路が一致していなかった可能性が高い。
- `SkyController` を `UnityEngine.InputSystem.Gyroscope.current` と `AttitudeSensor.current` に切り替え、`InputSystem.EnableDevice` で明示的に有効化した。
- `Gyroscope.angularVelocity` を基本入力、`AttitudeSensor.attitude` を予備入力として使用する。

## 2026-07-21 センサー・位置・時刻の再同期ボタン

- Androidの既存デバッグUIに `Re-sync GPS / time` ボタンを追加した。
- ボタン押下時は現在のカメラ表示を保持しながらジャイロ基準姿勢を再取得する。
- `LocationProvider` の最新GPS値を読み直し、次フレームで `DateTime.UtcNow` と恒星時を再計算する。

## 2026-07-21 星数削減とカメラ同期の安定化

- 星表を `stars_6` から `stars_5` に切り替え、生成対象を約5,080行から約1,630行へ削減した。
- 星生成時の行単位ログを削除し、生成件数だけを記録することで、起動時のログ負荷を下げた。
- Androidのカメラ背景とARセッションを再有効化した。ジャイロと `TrackedPoseDriver` の姿勢競合は避けるため、カメラ回転は引き続き `SkyController` が担当する。
- `AttitudeSensor` を優先し、利用できない端末だけ生ジャイロへフォールバックする。カメラ回転は `LateUpdate` で適用し、ジャイロの微小ノイズにはデッドゾーンとローパスを適用する。

## 2026-07-21 更新まとめ（現在のプロジェクト状態）

### 目的

観測者から見た星の位置を安定させ、場所・時刻による初期配置と恒星時の微小な変化だけを反映する。カメラ映像やARトラッキングが一時的に不安定でも、星の表示が停止・飛び・過度なドリフトを起こさない構成を目指す。

### 実装済み

- `MainARScene` はAR標準シーンを基盤として使用している。
- `ProjectSettings` は新Input System専用（`activeInputHandler: 1`）であり、センサー入力も新Input Systemへ統一した。
- `SkyController` は `AttitudeSensor` を優先し、利用できない場合だけ `Gyroscope` の角速度積分へフォールバックする。
- ジャイロの上下左右反転を補正し、カメラ回転は `LateUpdate` で適用する。生ジャイロ使用時は120Hz、ノイズデッドゾーン、ローパス処理を設定している。
- `TrackedPoseDriver` と `PlayerCameraController` はジャイロとの姿勢競合を避けるため無効化している。星空の天文計算とカメラ姿勢は別の責務として扱う。
- Androidの黒背景テストを解除し、AR Session、AR Camera Manager、AR Camera Backgroundを再有効化した。カメラ背景の初期フィルター濃度は0%である。
- `LocationProvider` はAndroid実機でGPS権限を要求し、実際の緯度・経度を取得する。Unity Editorでは大阪の固定地点をフォールバックとして使用する。
- `CelestialSphereController` は毎フレームの `DateTime.UtcNow` と現在経度から恒星時を計算し、緯度と合わせて天球へ反映する。通常の `siderealTimeScale` は1である。
- Android UIに `Re-sync GPS / time` ボタンを追加した。押下時は現在のカメラ表示を保持したまま、ジャイロ基準、GPS最新値、UTC、恒星時を再取得・再計算する。
- 星表を `stars_6` から `stars_5` へ変更し、生成対象を約5,080行から約1,630行へ削減した。星生成時の行単位ログも削除した。
- 恒星時、位置情報、緯度・経度、UTCをUnityログへ出力し、実機で同期状態を確認できるようにした。

### Sceneの主要設定

| 項目 | 現在値 |
|---|---|
| 星表 | `stars_5` |
| 恒星時倍率 | `1`（実時間） |
| Android黒背景テスト | 無効 |
| カメラ背景フィルター初期値 | `0` |
| AR Session Match Frame Rate | 無効 |
| ジャイロ反転補正 | 有効 |
| 融合済み姿勢センサー優先 | 有効 |
| 再同期ボタン | 有効 |

### 検証状況

- `dotnet build Assembly-CSharp.csproj --no-restore -t:Rebuild` はエラー0件で完了した。
- Unityパッケージ由来の既存警告は残っているが、今回の変更によるコンパイルエラーはない。
- Android実機では、カメラ映像、ジャイロ軸、星の安定性、再同期ボタン、GPS/UTCログを確認する必要がある。
